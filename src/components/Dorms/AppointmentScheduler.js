import React from "react";

import AppointmentForm from "./AppointmentForm";
const AppointmentScheduler = (props) => {
  return <AppointmentForm />; // calendar too
};

export default AppointmentScheduler;
